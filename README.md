# aicapstone hw5 - group 12
`course-affordance.ttl` is the imported file from TA.
`inferred-result.ttl` and `graspable_objects_output.txt` is generated by code.
other files are implemented by our team.